package com.tms.mcq.domain.model;

public enum MCQQuestionState {
    INSIATED, PROGRESS, COMPLETED
    // INSIATED = only Id is assigned
    // When all the fields persent = Completed other wise PROGRESS
}
